// Profile configuration
profiles {

    apptainer {
        apptainer.enabled = true
    }

    podman {
        podman.enabled = true
    }

    docker {
        docker.enabled = true
    }
}

// Executor configuration
executor {
    $local {
        cpus = 8
  }
}